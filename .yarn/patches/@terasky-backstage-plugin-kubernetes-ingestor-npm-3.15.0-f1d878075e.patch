diff --git a/config.d.ts b/config.d.ts
index d82f847c95a331a5da5a789ee5c8a4106f007031..19bdfa76102a8658f00bdc88aa2c4fc80a79a374 100644
--- a/config.d.ts
+++ b/config.d.ts
@@ -293,6 +293,11 @@ export interface Config {
          * @visibility frontend
          */
         ingestAllXRDs?: boolean;
+        /**
+         * Namespace in which generated namespaced Crossplane claims are hosted
+         * @visibility frontend
+         */
+        claimNamespace?: string;
         /**
          * Configuration for the publish phase in scaffolder templates
          */
@@ -334,6 +339,12 @@ export interface Config {
              * @visibility frontend
              */
             targetBranch?: string;
+            /**
+             * Subdirectory of the target repository to write the generated
+             * manifest into (e.g. "claims" for a shared app-config repo)
+             * @visibility frontend
+             */
+            targetPath?: string;
           };
         };
         /**
diff --git a/dist/providers/EntityProvider.cjs.js b/dist/providers/EntityProvider.cjs.js
index 31953fd9f6952f0f4585a761f591727b22acdfb4..64102601880642f2b3f5b49decabfb69c1ae7d19 100644
--- a/dist/providers/EntityProvider.cjs.js
+++ b/dist/providers/EntityProvider.cjs.js
@@ -584,6 +584,7 @@ class XRDTemplateEntityProvider {
   }
   extractParameters(version, clusters, xrd) {
     const normalizedClusters = clusters.map((cluster) => this.getNormalizedClusterName(cluster));
+    const claimNamespace = this.config.getOptionalString("kubernetesIngestor.crossplane.xrds.claimNamespace");
     const isV2 = !!xrd.spec?.scope;
     const scope = xrd.spec?.scope || (isV2 ? "LegacyCluster" : "Cluster");
     const isLegacyCluster = isV2 && scope === "LegacyCluster";
@@ -612,6 +613,13 @@ class XRDTemplateEntityProvider {
         maxLength: 63,
         type: "string"
       };
+      if (claimNamespace) {
+        Object.assign(mainParameterGroup.properties.xrNamespace, {
+          default: claimNamespace,
+          enum: [claimNamespace],
+          "ui:widget": "hidden"
+        });
+      }
     }
     mainParameterGroup.properties.owner = {
       title: "Owner",
@@ -1204,6 +1212,9 @@ class XRDTemplateEntityProvider {
       case "bitbucketcloud":
         action = "publish:bitbucketCloud:pull-request";
         break;
+      case "gitea":
+        action = "publish:gitea:pull-request";
+        break;
       case "github":
       default:
         action = "publish:github:pull-request";
@@ -1238,7 +1249,7 @@ class XRDTemplateEntityProvider {
     title: Create {KIND} Resource \${{ parameters.xrName }}
     description: Create {KIND} Resource \${{ parameters.xrName }}
     targetBranchName: ${this.config.getOptionalString("kubernetesIngestor.crossplane.xrds.publishPhase.git.targetBranch")}
-` + userOAuthTokenInput;
+` + (this.config.getOptionalString("kubernetesIngestor.crossplane.xrds.publishPhase.git.targetPath") ? `    targetPath: ${this.config.getOptionalString("kubernetesIngestor.crossplane.xrds.publishPhase.git.targetPath")}\n` : "") + userOAuthTokenInput;
         defaultStepsYaml += repoHardcodedStepsYaml;
       }
     }
@@ -1943,6 +1954,9 @@ class XRDTemplateEntityProvider {
       case "bitbucketcloud":
         action = "publish:bitbucketCloud:pull-request";
         break;
+      case "gitea":
+        action = "publish:gitea:pull-request";
+        break;
       case "github":
       default:
         action = "publish:github:pull-request";
